# Production composition

This directory documents the intended production boundary without creating infrastructure. A real production composition should use separate state, reviewed variables, GitHub environment protection, and an approved saved plan artifact. It must not reuse development state or credentials.

