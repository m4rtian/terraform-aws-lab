# Security Policy

Use GitHub's private vulnerability reporting for security concerns. Never open a public issue containing cloud credentials, account identifiers, state, plan output, resource addresses from a private environment, or production configuration.

This repository performs no automated apply. If a downstream deployment is affected, preserve the reviewed plan and state version, revoke exposed credentials, and follow the owning environment's incident process.
